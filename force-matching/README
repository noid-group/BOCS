This is the BOCS force matching toolkit. 
In order to build this, follow these steps:

$ mkdir build

$ cd build

$ cmake .. -G "Unix Makefiles" -DCMAKE_C_COMPILER=mpicc -DCMAKE_INSTALL_PREFIX=build

$ make

And the executables you want should be located in build/bin
